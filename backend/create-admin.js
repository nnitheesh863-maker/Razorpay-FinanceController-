const bcrypt = require('bcryptjs'); const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const salt = await bcrypt.genSalt(10); const passwordHash = await bcrypt.hash('Admin@1234', salt); const user = await prisma.user.upsert({ where: { email: 'admin@company.com' }, update: { passwordHash, role: 'ADMIN' }, create: { name: 'Demo Admin', email: 'admin@company.com', passwordHash, authProvider: 'EMAIL', role: 'ADMIN', emailVerified: true } }); console.log('Admin user ready:', user.email); } main().catch(console.error).finally(() => prisma.$disconnect());
